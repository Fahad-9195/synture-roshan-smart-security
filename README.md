# 🏠 Smart Security Absher

**نظام أمان منزلي ذكي متكامل — مع لوحة تحكم تفاعلية وواجهة API**

---

## 📌 نظرة سريعة

**Smart Security Absher** هو مشروع تعليمي متطور لمحاكاة أجهزة أمان منزلية ذكية (أقفال 🔓، حساسات حركة 🚨، حساسات غاز ☁️، كاميرات 📹) مع **لوحة تحكم ويب تفاعلية** تعرض الأحداث على:

- 🗺️ **خريطة ديناميكية** مع مواقع الأجهزة والأحداث
- 🔥 **خريطة حرارة** تعرض تركيز الأحداث الخطيرة
- 📊 **مخطط بياني حي** للأحداث عبر الزمن
- 📋 **جدول تفاعلي** لكل البلاغات والحوادث
- 🚔 **محاكاة دوريات** وحركة أمنية على الخريطة

**الهدف:** تجربة عملية لواجهات REST API، عرض بيانات الوقت الفعلي، وسيناريوهات محاكاة سريعة.

---

## 🎯 الميزات الرئيسية

✅ **منصة موحدة** — كل الأجهزة والأحداث في واجهة واحدة  
✅ **تنبيهات فورية** — إشعارات لحظية عند حدوث أي بلاغ  
✅ **فلاتر ذكية** — تصفية حسب المستوى والجهاز والوقت والحالة  
✅ **محاكاة سهلة** — تشغيل محاكاة من المتصفح أو سكربتات Python  
✅ **تصدير البيانات** — استخراج CSV وطباعة تقارير PDF  
✅ **وضعا العرض** — وضع صاحب المنزل (owner) ووضع عمليات (ops)  

---

## 📂 بنية المشروع

```
smart-security-absher/
├── backend/                    # خادم API
│   ├── main.py                # تطبيق FastAPI الرئيسي
│   ├── requirements.txt        # المكتبات المطلوبة
│   └── __pycache__/           # (لا يُرفع للـ Git)
│
├── dashboard/                  # لوحة التحكم (واجهة ويب)
│   ├── index.html             # الصفحة الرئيسية
│   ├── app.js                 # منطق التطبيق والخريطة
│   ├── styles.css             # أنماط التصميم
│   └── (مكتبات خارجية: Leaflet.js, Chart.js)
│
├── devices_simulation/         # محاكاة الأجهزة
│   ├── officer_tracker_simulation.py
│   ├── door_sensor.py
│   └── ...
│
├── .gitignore                 # استثناءات Git
├── LICENSE                    # رخصة MIT
└── README.md                  # هذا الملف
```

---

## ⚙️ المتطلبات

- **Python 3.10+** (مستحسن: 3.11 أو أحدث)
- **pip** لإدارة الحزم
- **متصفح حديث** (Chrome, Edge, Firefox)
- **Windows PowerShell** (أو أي terminal آخر)

---

## 🚀 تشغيل المشروع محليًا

### الخطوة 1: تشغيل الخادم (Backend)

افتح PowerShell وانتقل إلى مجلد `backend`:

```powershell
cd C:\Users\abdal\Desktop\smart-security-absher\backend

# إنشاء بيئة افتراضية
python -m venv .venv

# تفعيل البيئة الافتراضية
.\.venv\Scripts\Activate.ps1

# تثبيت المكتبات
pip install -r requirements.txt

# تشغيل الخادم
uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

✅ الخادم الآن يعمل على: `http://127.0.0.1:8000`  
📖 عرض المستندات التفاعلية: `http://127.0.0.1:8000/docs`

### الخطوة 2: تشغيل لوحة التحكم (Dashboard)

افتح نافذة PowerShell جديدة:

```powershell
cd C:\Users\abdal\Desktop\smart-security-absher\dashboard

# تشغيل خادم ثابت
python -m http.server 5500
```

✅ الواجهة الآن على: `http://127.0.0.1:5500`

تأكد أن المتغير `API_URL` في `dashboard/app.js` (السطر الأول) يشير إلى:
```javascript
const API_URL = "http://127.0.0.1:8000/api/events";
```

---

## 🔌 واجهة برمجة التطبيقات (API Reference)

### نقاط النهاية (Endpoints)

#### 1. الحصول على كل الأحداث
```http
GET /api/events
```

**الاستجابة:**
```json
[
  {
    "id": 1,
    "device_id": "door_sensor_1",
    "type": "door_open",
    "level": "warning",
    "timestamp": "2025-12-09T12:34:56.000Z",
    "status": "open",
    "home_id": "HOME-1234",
    "absher_id": "1234567890"
  }
]
```

#### 2. إضافة حدث جديد
```http
POST /api/events
Content-Type: application/json

{
  "device_id": "motion_sensor_1",
  "type": "motion_detected",
  "level": "danger",
  "timestamp": "2025-12-09T12:35:00.000Z",
  "home_id": "HOME-1234",
  "absher_id": "1234567890"
}
```

#### 3. تعليم كل البلاغات المفتوحة كمحلولة
```http
POST /api/events/resolve_all
```

**الاستجابة:**
```json
{
  "resolved": 5
}
```

#### 4. مسح كل الأحداث (للاختبار)
```http
POST /api/events/reset
```

### أمثلة الطلبات (PowerShell)

**إرسال حدث عبر PowerShell:**

```powershell
$body = @{
    device_id = "door_sensor_1"
    type = "door_open"
    level = "warning"
    timestamp = (Get-Date -Format o)
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://127.0.0.1:8000/api/events" `
                  -Method Post `
                  -Body $body `
                  -ContentType 'application/json'
```

**جلب كل الأحداث:**

```powershell
Invoke-RestMethod -Uri "http://127.0.0.1:8000/api/events" -Method Get
```

---

## 🎮 المحاكاة (Simulation)

### المحاكاة من المتصفح

في لوحة التحكم، انقر على الزر **"▶ تشغيل المحاكاة من المتصفح"** لإرسال أحداث عشوائية كل 2 ثانية.

### المحاكاة من السيرفر (Python)

افتح نافذة PowerShell جديدة:

```powershell
cd C:\Users\abdal\Desktop\smart-security-absher\devices_simulation

# تشغيل سكربت محاكاة الأجهزة
python officer_tracker_simulation.py
```

---

## 📊 ميزات لوحة التحكم

| الميزة | الوصف |
|--------|-------|
| **الخريطة الديناميكية** | عرض الأجهزة والأحداث بألوان حسب المستوى |
| **الخريطة الحرارية** | تراكم الأحداث الخطيرة في مناطق معينة |
| **مخطط بياني** | رسم بياني لحي للأحداث عبر الوقت |
| **فلاتر متقدمة** | تصفية حسب المستوى/الجهاز/الوقت/الحالة |
| **الجدول التفاعلي** | قائمة كاملة بكل الأحداث |
| **التنبيهات** | تنبيهات صوتية وبصرية للأحداث الخطيرة |
| **التصدير** | حفظ البيانات كـ CSV وطباعة PDF |
| **الثيم** | تبديل بين الوضع الفاتح والداكن |

---

## 🛠 استكشاف الأخطاء وإصلاحها

### الخادم لا يستجيب
```powershell
# تأكد من تشغيل الخادم
# تحقق من المنفذ 8000
netstat -ano | findstr :8000
```

### المتصفح لا يستطيع الاتصال بـ API
- ✅ تأكد أن الخادم يعمل على المنفذ 8000
- ✅ تحقق من قيمة `API_URL` في `dashboard/app.js`
- ✅ افتح Developer Console (`F12`) وشاهد الأخطاء

### البيانات لا تتحدث
- اضغط على زر **"▶ تشغيل التحديث"** في الواجهة
- تأكد من تشغيل المحاكاة (زر **"▶ تشغيل المحاكاة"**)

---

## 📤 نشر المشروع على GitHub

إذا أردت رفع التغييرات إلى GitHub:

```powershell
cd C:\Users\abdal\Desktop\smart-security-absher

git add .
git commit -m "تحديث المشروع — إضافة ميزات جديدة"
git push origin main
```

---

## 🔐 الرخصة

هذا المشروع مرخص تحت **رخصة MIT** — انظر ملف `LICENSE` للتفاصيل.

---

## 👨‍💻 ملاحظات للمطورين

### الملفات المهمة
- `backend/main.py` — نقطة الانطلاق للـ API
- `dashboard/app.js` — منطق التطبيق والخريطة (920+ سطر)
- `dashboard/index.html` — البنية الأساسية للصفحة

### المكتبات المستخدمة
- **FastAPI** — إطار العمل الخلفي
- **Leaflet.js** — خرائط تفاعلية
- **Chart.js** — رسوم بيانية
- **Pydantic** — التحقق من البيانات

### نصائح التطوير
- استخدم `uvicorn main:app --reload` للتطوير لإعادة تحميل تلقائية
- افتح `http://127.0.0.1:8000/docs` لاختبار API بشكل تفاعلي
- افتح Developer Console (`F12`) لرؤية الأخطاء والسجلات

---

## 📞 التواصل والدعم

إذا واجهت أي مشكلة أو أردت إضافة ميزة:
- افتح **Issue** في المستودع
- شارك ملاحظاتك وأفكارك

---

**شكراً لاستخدامك Smart Security Absher! 🚀**
