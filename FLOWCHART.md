# 📊 Flow Chart - نظام الأمان الذكي أبشر

## نظرة شاملة على تدفق البيانات والعمليات

---

## 🌐 1. نقطة البداية (Entry Point)

```
┌─────────────────────────────────────┐
│   المستخدم يفتح الموقع             │
│   https://syntrue-absher.onrender.com │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│      صفحة الترحيب (Welcome)         │
│      /static/welcome.html            │
│                                       │
│   [زر تبديل اللغة: عربي/إنجليزي]    │
└──────────────┬──────────────────────┘
               │
        ┌──────┴──────┐
        │             │
        ▼             ▼
   ┌─────────┐   ┌─────────┐
   │ أفراد   │   │ عسكري   │
   └────┬────┘   └────┬────┘
        │             │
```

---

## 👨‍👩‍👦 2. مسار الأفراد (Civilian Flow)

```
┌────────────────────────────────────┐
│   صفحة تسجيل دخول الأفراد          │
│   /static/home-login.html           │
└──────────────┬─────────────────────┘
               │
        ┌──────┴──────┐
        │ المصادقة     │
        │ POST /api/auth/login │
        └──────┬──────┘
               │
        ┌──────┴──────┐
        │   صحيح؟     │
        └──────┬──────┘
         │           │
      نعم│           │لا
         │           │
         ▼           ▼
   ┌─────────┐   ┌──────────┐
   │ نجاح    │   │ خطأ      │
   │ JWT     │   │ رسالة    │
   └────┬────┘   └──────────┘
        │
        ▼
┌────────────────────────────────────┐
│   لوحة تحكم الأفراد                │
│   /static/dashboard.html            │
│                                      │
│   📊 الإحصائيات:                   │
│   - حالة النظام                    │
│   - الأجهزة النشطة (24 ساعة)       │
│   - التنبيهات المعلقة              │
│   - الأحداث المسجلة                │
└──────────────┬─────────────────────┘
               │
        ┌──────┴──────────┐
        │                 │
        ▼                 ▼
   ┌─────────┐      ┌──────────┐
   │ الأجهزة │      │ الأحداث  │
   └────┬────┘      └─────┬────┘
        │                 │
        ▼                 ▼
   GET /api/devices  GET /api/events
```

### وظائف لوحة الأفراد:

1. **عرض الأجهزة:**
   - كاميرات المراقبة
   - مستشعرات الأبواب
   - مستشعرات الحركة
   - كواشف الدخان

2. **إدارة الأحداث:**
   - عرض الأحداث الأخيرة
   - تصفية حسب النوع والحالة
   - تحديث الحالة (حل/معلق)
   - تصدير التقارير CSV

3. **البلاغات:**
   - إرسال بلاغ للشرطة
   - تتبع حالة البلاغ
   - سجل البلاغات السابقة

---

## 🚔 3. مسار العسكريين (Military Flow)

```
┌────────────────────────────────────┐
│   صفحة تسجيل دخول العمليات         │
│   /static/operations-login.html     │
└──────────────┬─────────────────────┘
               │
        ┌──────┴──────┐
        │ المصادقة     │
        │ POST /api/auth/login │
        └──────┬──────┘
               │
        ┌──────┴──────┐
        │   صحيح؟     │
        └──────┬──────┘
         │           │
      نعم│           │لا
         │           │
         ▼           ▼
   ┌─────────┐   ┌──────────┐
   │ نجاح    │   │ خطأ      │
   │ JWT     │   │ رسالة    │
   └────┬────┘   └──────────┘
        │
        ▼
┌────────────────────────────────────┐
│   مركز العمليات الميدانية          │
│   /static/operations-center.html    │
│                                      │
│   📊 معلومات فورية:                │
│   - ضباط في الخدمة                 │
│   - دوريات نشطة                    │
│   - متوسط الاستجابة (دقائق)        │
│   - حوادث اليوم                    │
└──────────────┬─────────────────────┘
               │
      ┌────────┴────────┬──────────┐
      │                 │          │
      ▼                 ▼          ▼
┌──────────┐    ┌──────────┐  ┌─────────┐
│ الضباط   │    │ الحوادث  │  │ الفعاليات│
└─────┬────┘    └─────┬────┘  └────┬────┘
      │               │            │
      ▼               ▼            ▼
GET /api/      GET /api/     /static/
officers       incidents     events-dashboard.html
```

### وظائف مركز العمليات:

1. **إدارة الدوريات:**
   - تتبع موقع الضباط (GPS)
   - حالة الدورية (نشط/متاح/خارج الخدمة)
   - آخر بلاغ مستلم

2. **إدارة الحوادث:**
   - عرض الحوادث النشطة
   - تعيين ضابط للحادثة
   - تحديث حالة الحادثة
   - تقارير الحوادث

3. **الفعاليات الموسمية:**
   - إدارة فعاليات الحج/العمرة
   - تتبع المشاركين
   - التنبيهات الأمنية

---

## 🎪 4. نظام الفعاليات الموسمية (Events Management)

```
┌────────────────────────────────────┐
│   لوحة إدارة الفعاليات              │
│   /static/events-dashboard.html     │
└──────────────┬─────────────────────┘
               │
      ┌────────┴────────┐
      │                 │
      ▼                 ▼
┌──────────┐      ┌──────────┐
│ حج/عمرة  │      │ فعاليات │
│          │      │ دينية    │
└────┬─────┘      └─────┬────┘
     │                  │
     ▼                  ▼
POST /api/events/seasonal
     │
     ├─────┬─────────┬──────────┬──────────┐
     │     │         │          │          │
     ▼     ▼         ▼          ▼          ▼
  ┌────┐ ┌────┐  ┌────┐    ┌────┐    ┌────┐
  │مشار│ │أجهز│  │بصما│    │تتبع│    │تنبي│
  │كين│ │ة   │  │ت   │    │الدخ│    │هات│
  │    │ │IoT │  │    │    │ول  │    │أمن │
  └────┘ └────┘  └────┘    └────┘    └────┘
     │     │       │          │          │
     └─────┴───────┴──────────┴──────────┘
                    │
                    ▼
             ┌──────────────┐
             │  التقارير     │
             │  والإحصائيات │
             └──────────────┘
```

### مكونات نظام الفعاليات:

#### 1. إدارة الفعاليات:
- **إنشاء فعالية:**
  - اسم الفعالية (حج، عمرة، مناسبة دينية)
  - تاريخ البداية والنهاية
  - الموقع (GPS)
  - عدد المشاركين المتوقع
  - مستوى الأمان

- **معلومات الفعالية:**
  - إجمالي المشاركين
  - المسجلين
  - التحقق منهم
  - الحاضرين حالياً

#### 2. إدارة المشاركين:
```
POST /api/events/seasonal/{event_id}/participants
├── معلومات شخصية (اسم، جواز، جوال)
├── التحقق من الهوية
├── تسجيل بيانات بيومترية
│   ├── بصمة الإصبع
│   ├── التعرف على الوجه
│   └── مسح قزحية العين
└── إصدار معرف دخول
    ├── QR Code
    ├── NFC Card
    └── RFID Bracelet
```

#### 3. أجهزة IoT:
- **أنواع الأجهزة:**
  - أساور ذكية (Smart Bracelets)
  - بطاقات NFC
  - أجهزة RFID

- **المعلومات المتتبعة:**
  - موقع GPS
  - مستوى البطارية
  - آخر اتصال
  - إشارات الطوارئ

#### 4. تتبع الدخول والخروج:
```
POST /api/events/seasonal/{event_id}/access-log
{
  "participant_id": "P123456",
  "checkpoint_id": "GATE_A",
  "access_type": "entry/exit",
  "timestamp": "2025-12-11T10:30:00"
}
```

#### 5. التنبيهات الأمنية:
- **أنواع التنبيهات:**
  - محاولة دخول غير مصرح
  - اكتشاف احتيال
  - فقدان إشارة جهاز IoT
  - تجاوز سعة الموقع

---

## 🔐 5. نظام المصادقة (Authentication Flow)

```
┌─────────────────────┐
│  طلب تسجيل دخول    │
│  POST /api/auth/login│
│  {username, password}│
└──────────┬──────────┘
           │
           ▼
    ┌──────────────┐
    │ البحث في DB  │
    │ users table  │
    └──────┬───────┘
           │
    ┌──────┴──────┐
    │ موجود؟      │
    └──────┬──────┘
      │          │
    لا│          │نعم
      │          │
      ▼          ▼
  ┌──────┐  ┌────────────┐
  │ 401  │  │ التحقق من  │
  │Error │  │ كلمة المرور│
  └──────┘  └──────┬─────┘
                   │
            ┌──────┴──────┐
            │ hash match? │
            └──────┬──────┘
              │          │
            لا│          │نعم
              │          │
              ▼          ▼
          ┌──────┐  ┌────────────┐
          │ 401  │  │ إنشاء JWT  │
          │Error │  │ Token      │
          └──────┘  └──────┬─────┘
                           │
                           ▼
                    ┌──────────────┐
                    │ تسجيل النشاط │
                    │ في log       │
                    └──────┬───────┘
                           │
                           ▼
                    ┌──────────────┐
                    │ إرجاع Token │
                    │ + معلومات   │
                    │ المستخدم    │
                    └──────────────┘
```

### JWT Token Structure:
```json
{
  "user_id": 123,
  "username": "admin",
  "role": "admin",
  "exp": "2025-12-12T10:30:00"
}
```

### الأدوار (Roles):
- **admin**: صلاحيات كاملة
- **supervisor**: مشرف أمني
- **operator**: مشغل العمليات
- **viewer**: عرض فقط

---

## 📡 6. API Request Flow (مع Middleware)

```
┌─────────────────────┐
│  HTTP Request       │
│  من المستعرض         │
└──────────┬──────────┘
           │
           ▼
┌──────────────────────┐
│ SecurityHeaders      │ ← إضافة رؤوس الأمان
│ Middleware           │   X-Frame-Options
│                      │   X-Content-Type-Options
└──────────┬───────────┘   Strict-Transport-Security
           │
           ▼
┌──────────────────────┐
│ RateLimit            │ ← فحص الحد الأقصى
│ Middleware           │   (100 طلب/دقيقة/IP)
└──────────┬───────────┘
           │
    ┌──────┴──────┐
    │ تجاوز الحد؟ │
    └──────┬──────┘
      │          │
    نعم│         │لا
      │          │
      ▼          ▼
  ┌──────┐  ┌──────────────┐
  │ 429  │  │ RequestLogging│ ← تسجيل الطلب
  │Error │  │ Middleware    │   + قياس الوقت
  └──────┘  └──────┬────────┘
                   │
                   ▼
           ┌───────────────┐
           │ ErrorHandler  │ ← معالجة الأخطاء
           │ Middleware    │   المركزية
           └───────┬───────┘
                   │
                   ▼
           ┌───────────────┐
           │ CORS          │ ← السماح بـ
           │ Middleware    │   Cross-Origin
           └───────┬───────┘
                   │
                   ▼
           ┌───────────────┐
           │ Route Handler │ ← معالج الطلب
           │ (Endpoint)    │   الفعلي
           └───────┬───────┘
                   │
            ┌──────┴──────┐
            │  نجاح؟      │
            └──────┬──────┘
              │          │
            لا│          │نعم
              │          │
              ▼          ▼
          ┌──────┐  ┌──────────┐
          │ Error│  │ Response │
          │ 500  │  │ 200 OK   │
          └──┬───┘  └────┬─────┘
             │           │
             └─────┬─────┘
                   │
                   ▼
           ┌───────────────┐
           │ تسجيل في Log  │
           │ app.log       │
           │ error.log     │
           └───────┬───────┘
                   │
                   ▼
           ┌───────────────┐
           │ HTTP Response │
           │ + Headers     │
           │ X-Process-Time│
           └───────────────┘
```

---

## 💾 7. Database Schema

```
┌─────────────────────────────────────────────────────────┐
│                   SQLite Database                        │
│                  (12 Tables Total)                       │
└─────────────────────────────────────────────────────────┘

┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│    users     │  │   events     │  │   devices    │
├──────────────┤  ├──────────────┤  ├──────────────┤
│ id (PK)      │  │ id (PK)      │  │ id (PK)      │
│ username     │  │ timestamp    │  │ device_id    │
│ password_hash│  │ device_id    │  │ type         │
│ role         │  │ type         │  │ location     │
│ full_name    │  │ level        │  │ status       │
│ email        │  │ status       │  │ battery      │
│ is_active    │  │ home_id      │  │ last_update  │
│ created_at   │  │ absher_id    │  └──────────────┘
└──────────────┘  │ location     │
                  └──────────────┘

┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  officers    │  │ resolutions  │  │activity_log  │
├──────────────┤  ├──────────────┤  ├──────────────┤
│ id (PK)      │  │ id (PK)      │  │ id (PK)      │
│ name         │  │ event_id     │  │ user_id (FK) │
│ badge_number │  │ resolution   │  │ action       │
│ rank         │  │ resolved_by  │  │ details      │
│ status       │  │ timestamp    │  │ ip_address   │
│ location     │  │ notes        │  │ timestamp    │
│ last_reported│  └──────────────┘  └──────────────┘
└──────────────┘

┌────────────────────┐  ┌────────────────────┐
│ seasonal_events    │  │   participants     │
├────────────────────┤  ├────────────────────┤
│ id (PK)            │  │ id (PK)            │
│ event_name         │  │ event_id (FK)      │
│ event_type         │  │ participant_id     │
│ description        │  │ full_name          │
│ start_date         │  │ national_id        │
│ end_date           │  │ passport_number    │
│ location_lat       │  │ phone              │
│ location_lng       │  │ verification_status│
│ max_participants   │  │ created_at         │
│ security_level     │  └────────────────────┘
│ status             │
└────────────────────┘

┌────────────────────┐  ┌────────────────────┐
│   iot_devices      │  │  biometric_data    │
├────────────────────┤  ├────────────────────┤
│ id (PK)            │  │ id (PK)            │
│ event_id (FK)      │  │ participant_id (FK)│
│ device_id          │  │ fingerprint_hash   │
│ device_type        │  │ face_encoding      │
│ participant_id (FK)│  │ iris_scan          │
│ battery_level      │  │ confidence_score   │
│ last_location      │  │ created_at         │
│ status             │  └────────────────────┘
└────────────────────┘

┌────────────────────┐  ┌────────────────────┐
│   access_logs      │  │ security_alerts    │
├────────────────────┤  ├────────────────────┤
│ id (PK)            │  │ id (PK)            │
│ event_id (FK)      │  │ event_id (FK)      │
│ participant_id (FK)│  │ alert_type         │
│ checkpoint_id      │  │ severity           │
│ access_type        │  │ description        │
│ timestamp          │  │ location           │
│ device_used        │  │ status             │
└────────────────────┘  │ created_at         │
                        └────────────────────┘
```

---

## 🔄 8. Real-time Updates Flow

```
┌─────────────────────────────────────────────────────┐
│              Frontend (Dashboard)                    │
└───────────────────┬─────────────────────────────────┘
                    │
                    ▼
            ┌───────────────┐
            │ Page Load     │
            │ document.ready│
            └───────┬───────┘
                    │
                    ▼
            ┌───────────────┐
            │ startPolling()│
            └───────┬───────┘
                    │
                    ▼
            ┌───────────────┐
            │ setInterval   │
            │ (every 3 sec) │
            └───────┬───────┘
                    │
                    ▼
        ┌───────────────────────┐
        │ AJAX Request          │
        │ GET /api/events       │
        │ GET /api/statistics   │
        │ GET /api/officers     │
        └───────┬───────────────┘
                │
                ▼
        ┌───────────────┐
        │ Backend       │
        │ Processing    │
        └───────┬───────┘
                │
                ▼
        ┌───────────────┐
        │ Query Database│
        └───────┬───────┘
                │
                ▼
        ┌───────────────┐
        │ Return JSON   │
        └───────┬───────┘
                │
                ▼
        ┌───────────────┐
        │ Frontend      │
        │ Receives Data │
        └───────┬───────┘
                │
    ┌───────────┴────────────┐
    │                        │
    ▼                        ▼
┌─────────┐          ┌──────────────┐
│ Compare │          │ New Events?  │
│ with    │          │ Changes?     │
│ Previous│          └──────┬───────┘
└────┬────┘                 │
     │                      │
     └──────────┬───────────┘
                │
         ┌──────┴──────┐
         │ Changes?    │
         └──────┬──────┘
           │          │
         نعم│         │لا
           │          │
           ▼          ▼
    ┌──────────┐  ┌────────┐
    │ Update   │  │ Skip   │
    │ UI       │  └────────┘
    │ - Table  │
    │ - Stats  │
    │ - Charts │
    │ - Sound  │
    └────┬─────┘
         │
         ▼
    ┌──────────┐
    │ Wait 3s  │
    └────┬─────┘
         │
         └──────┐ (Loop)
                │
                └────────┘
```

---

## 📊 9. Complete System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                      🌐 Frontend Layer                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │ welcome  │ │dashboard │ │operations│ │  events  │          │
│  │  .html   │ │  .html   │ │  .html   │ │  .html   │          │
│  │          │ │          │ │          │ │          │          │
│  │ [AR/EN]  │ │ [Polling]│ │ [Polling]│ │ [CRUD]   │          │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘          │
└───────┼────────────┼────────────┼────────────┼─────────────────┘
        │            │            │            │
        └────────────┴────────────┴────────────┘
                     │ HTTPS/API
┌────────────────────┼──────────────────────────────────────────────┐
│                    ▼         🛡️ Middleware Layer                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │ Security │ │   Rate   │ │ Request  │ │  Error   │           │
│  │ Headers  │ │ Limiting │ │ Logging  │ │ Handler  │           │
│  │          │ │          │ │          │ │          │           │
│  │ XSS Prot │ │ 100/min  │ │ app.log  │ │ Try/Catch│           │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘           │
└───────┼────────────┼────────────┼────────────┼──────────────────┘
        └────────────┴────────────┴────────────┘
                     │
┌────────────────────┼──────────────────────────────────────────────┐
│                    ▼      ⚙️ Application Layer                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              main.py (FastAPI App)                         │ │
│  │                                                             │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐     │ │
│  │  │   Auth   │ │  Events  │ │ Officers │ │ Seasonal │     │ │
│  │  │   API    │ │   API    │ │   API    │ │   API    │     │ │
│  │  │          │ │          │ │          │ │          │     │ │
│  │  │ /login   │ │ /events  │ │ /officers│ │ /seasonal│     │ │
│  │  │ /register│ │ /devices │ │ /incidents│ │ /particip│     │ │
│  │  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘     │ │
│  └───────┼────────────┼────────────┼────────────┼───────────┘ │
└──────────┼────────────┼────────────┼────────────┼──────────────┘
           │            │            │            │
┌──────────┼────────────┼────────────┼────────────┼──────────────┐
│          ▼            ▼            ▼            ▼  📦 Logic     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐         │
│  │ auth.py  │ │database  │ │  events  │ │ config   │         │
│  │          │ │  .py     │ │_management│ │  .py     │         │
│  │          │ │          │ │   .py     │ │          │         │
│  │ JWT Gen  │ │ CRUD Ops │ │ Events DB│ │ Settings │         │
│  │ Password │ │ SQLite   │ │ IoT Track│ │ Logging  │         │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘         │
└───────┼────────────┼────────────┼────────────┼────────────────┘
        │            │            │            │
┌───────┼────────────┼────────────┼────────────┼────────────────┐
│       ▼            ▼            ▼            ▼  💾 Data        │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │          SQLite Database (security_system.db)            │ │
│  │                                                           │ │
│  │  ┌────────┐ ┌────────┐ ┌─────────┐ ┌──────────┐        │ │
│  │  │ users  │ │ events │ │ devices │ │ officers │        │ │
│  │  └────────┘ └────────┘ └─────────┘ └──────────┘        │ │
│  │                                                           │ │
│  │  ┌────────┐ ┌────────┐ ┌─────────┐ ┌──────────┐        │ │
│  │  │seasonal│ │particip│ │   iot   │ │biometric │        │ │
│  │  │_events │ │  ants  │ │ devices │ │   data   │        │ │
│  │  └────────┘ └────────┘ └─────────┘ └──────────┘        │ │
│  │                                                           │ │
│  │  ┌────────┐ ┌────────┐ ┌─────────┐ ┌──────────┐        │ │
│  │  │ access │ │security│ │activity │ │resolution│        │ │
│  │  │  logs  │ │ alerts │ │   log   │ │    s     │        │ │
│  │  └────────┘ └────────┘ └─────────┘ └──────────┘        │ │
│  └──────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🔒 10. Security & Error Handling Flow

```
┌─────────────────────┐
│   User Action       │
│   (Any Request)     │
└──────────┬──────────┘
           │
           ▼
    ┌──────────────┐
    │ Requires     │
    │ Auth?        │
    └──────┬───────┘
      │          │
    لا│          │نعم
      │          │
      ▼          ▼
  ┌──────┐  ┌────────────┐
  │ Allow│  │ Check JWT  │
  └──┬───┘  │ Token      │
     │      └──────┬─────┘
     │             │
     │      ┌──────┴──────┐
     │      │ Valid?      │
     │      └──────┬──────┘
     │        │          │
     │      لا│          │نعم
     │        │          │
     │        ▼          ▼
     │    ┌──────┐  ┌────────┐
     │    │ 401  │  │ Check  │
     │    │Unauth│  │ Role   │
     │    └──────┘  └────┬───┘
     │                   │
     │            ┌──────┴──────┐
     │            │ Allowed?    │
     │            └──────┬──────┘
     │              │          │
     │            لا│          │نعم
     │              │          │
     │              ▼          │
     │          ┌──────┐       │
     │          │ 403  │       │
     │          │Forbid│       │
     │          └──────┘       │
     │                         │
     └─────────────┬───────────┘
                   │
                   ▼
           ┌───────────────┐
           │ Process       │
           │ Request       │
           └───────┬───────┘
                   │
            ┌──────┴──────┐
            │ Try/Catch   │
            └──────┬──────┘
              │          │
           Success│      │Error
              │          │
              ▼          ▼
          ┌──────┐  ┌──────────┐
          │ 200  │  │ Log Error│
          │  OK  │  │ error.log│
          └──────┘  └────┬─────┘
                         │
                         ▼
                  ┌──────────────┐
                  │ Return 500   │
                  │ + Arabic Msg │
                  └──────────────┘
```

---

## 📈 11. Performance & Caching Strategy

```
┌─────────────────────┐
│  Client Request     │
│  GET /api/statistics│
└──────────┬──────────┘
           │
           ▼
    ┌──────────────┐
    │ Check Cache  │
    │ @lru_cache   │
    └──────┬───────┘
           │
    ┌──────┴──────┐
    │ In Cache?   │
    └──────┬──────┘
      │          │
    نعم│         │لا
      │          │
      ▼          ▼
  ┌──────┐  ┌────────────┐
  │Return│  │ Query DB   │
  │Cached│  │            │
  │ Data │  └──────┬─────┘
  └──┬───┘         │
     │             ▼
     │      ┌──────────────┐
     │      │ Calculate    │
     │      │ Statistics   │
     │      └──────┬───────┘
     │             │
     │             ▼
     │      ┌──────────────┐
     │      │ Store in     │
     │      │ Cache (5min) │
     │      └──────┬───────┘
     │             │
     └──────┬──────┘
            │
            ▼
     ┌──────────────┐
     │ Return Data  │
     │ + Headers    │
     │ X-Cache: HIT │
     └──────────────┘
```

### Cached Endpoints:
- `GET /api/statistics` - 5 دقائق
- `GET /api/resolutions/stats` - 5 دقائق
- `GET /api/officers` - 3 دقائق

---

## 🚨 12. Alert & Notification Flow

```
┌─────────────────────┐
│  Event Detected     │
│  (High Priority)    │
└──────────┬──────────┘
           │
           ▼
    ┌──────────────┐
    │ Check Level  │
    └──────┬───────┘
           │
    ┌──────┴──────────────┐
    │                     │
    ▼                     ▼
┌────────┐          ┌──────────┐
│ Normal │          │ Critical │
│ Event  │          │  Event   │
└────┬───┘          └────┬─────┘
     │                   │
     ▼                   ▼
┌────────┐      ┌─────────────────┐
│ Log to │      │ Create Alert    │
│ events │      │ + Sound         │
│ table  │      │ + Visual        │
└────┬───┘      │ + Database      │
     │          └────┬────────────┘
     │               │
     │               ▼
     │        ┌──────────────┐
     │        │ Assign to    │
     │        │ Nearest      │
     │        │ Officer      │
     │        └──────┬───────┘
     │               │
     │               ▼
     │        ┌──────────────┐
     │        │ Send         │
     │        │ Notification │
     │        └──────┬───────┘
     │               │
     └───────┬───────┘
             │
             ▼
      ┌──────────────┐
      │ Update       │
      │ Dashboard    │
      │ (Real-time)  │
      └──────────────┘
```

---

## 📱 13. Mobile & Responsive Flow

```
┌─────────────────────┐
│  Device Detection   │
└──────────┬──────────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
┌────────┐   ┌────────┐
│Desktop │   │ Mobile │
└────┬───┘   └────┬───┘
     │            │
     ▼            ▼
┌────────┐   ┌────────┐
│ Full   │   │Simplified│
│ Layout │   │  Layout │
└────┬───┘   └────┬───┘
     │            │
     └─────┬──────┘
           │
           ▼
    ┌──────────────┐
    │ CSS Media    │
    │ Queries      │
    │ @media       │
    └──────┬───────┘
           │
           ▼
    ┌──────────────┐
    │ Responsive   │
    │ Components   │
    │ - Tables     │
    │ - Cards      │
    │ - Buttons    │
    └──────────────┘
```

---

## 🔄 14. Data Export/Import Flow

```
┌─────────────────────┐
│  User Action        │
└──────────┬──────────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
┌────────┐   ┌────────┐
│ Export │   │ Import │
└────┬───┘   └────┬───┘
     │            │
     ▼            ▼
┌────────────┐  ┌────────────┐
│ GET Data   │  │ Read File  │
│ from DB    │  │ (CSV)      │
└──────┬─────┘  └──────┬─────┘
       │               │
       ▼               ▼
┌────────────┐  ┌────────────┐
│ Convert to │  │ Parse CSV  │
│ CSV Format │  │ Validate   │
└──────┬─────┘  └──────┬─────┘
       │               │
       ▼               ▼
┌────────────┐  ┌────────────┐
│ Generate   │  │ Insert to  │
│ Download   │  │ Database   │
│ events.csv │  └──────┬─────┘
└────────────┘         │
                       ▼
                ┌────────────┐
                │ Return     │
                │ Success    │
                │ Count      │
                └────────────┘
```

---

## ✅ Summary

### النظام يتكون من:

1. **Frontend**: HTML + CSS + JavaScript
2. **Backend**: Python + FastAPI
3. **Database**: SQLite (12 tables)
4. **Middleware**: 5 layers للأمان والأداء
5. **APIs**: 30+ endpoint
6. **Features**:
   - نظام مصادقة JWT
   - تتبع فوري للأحداث
   - إدارة فعاليات موسمية
   - أجهزة IoT
   - بيانات بيومترية
   - تقارير وإحصائيات
   - ترجمة عربي/إنجليزي

### الأداء:
- Response Time: ~80ms
- Caching: 5 minutes TTL
- Rate Limit: 100 req/min
- Real-time Updates: 3 seconds

---

**📅 التاريخ:** 11 ديسمبر 2025  
**👨‍💻 المطور:** Syntrue Team  
**📌 الإصدار:** 2.0  
**🌐 الموقع:** https://syntrue-absher.onrender.com
